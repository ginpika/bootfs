# 你的 S3 访问密钥
SERVER_ENDPOINT=host.docker.internal:8181/s3
ACCESS_KEY=bootfs
SECRET_KEY=bootfs-secret-dev

# 是否启用 HTTPS (true / false)
ENABLE_HTTPS=false

# 随意指定一个用于测试的 Bucket 名字（Mint 会自动创建和删除它）
MINT_MODE=bootfs